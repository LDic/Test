using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sc_Camera : MonoBehaviour
{
	
	// Update is called once per frame
	void Update ()
	{
		//GetComponent<Camera>().transform.rotation = new Quaternion();
	}
}
